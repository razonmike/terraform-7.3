Домашнее задание к занятию "7.2. Облачные провайдеры и синтаксис Terraform."

Задача 1 (Вариант с Yandex.Cloud). Регистрация в ЯО и знакомство с основами (необязательно, но крайне желательно).
4.Воспользуйтесь инструкцией на сайте терраформа, что бы не указывать авторизационный токен в коде, а терраформ провайдер брал его из переменных окружений.
```bash
export TF_VAR_yc_token=AQAAAAAC...
export TF_VAR_yc_cloud_id=b1gs8...
export TF_VAR_yc_folder_id=b1get0pj...
export TF_VAR_yc_service_account_id=ajeihmjv3r...
export TF_VAR_yc_zone="ru-central1-a"

и тоже самое для packer export PKR_VAR_yc_ и так далее

```
Задача 2. Создание yandex_compute_instance через терраформ.
В качестве результата задания предоставьте:

Ответ на вопрос: при помощи какого инструмента (из разобранных на прошлом занятии) можно создать свой образ ami?
```
Packer создает AMI, Ansible конфигурирует окружение внутри образа, Terraform разворачивает инстансы используя созданный образ с помощью Packer
```
Ссылку на репозиторий с исходной конфигурацией терраформа.

https://github.com/razonmike/netology-7.2